// ignore_for_file: public_member_api_docs

extension type const SideNavStreamType(int value) implements int {
  static const SideNavStreamType collections = SideNavStreamType(1);
  static const SideNavStreamType readingLists = SideNavStreamType(2);
  static const SideNavStreamType bookmarks = SideNavStreamType(3);
  static const SideNavStreamType library = SideNavStreamType(4);
  static const SideNavStreamType smartFilter = SideNavStreamType(5);
  static const SideNavStreamType externalSource = SideNavStreamType(6);
  static const SideNavStreamType allSeries = SideNavStreamType(7);
  static const SideNavStreamType wantToRead = SideNavStreamType(8);
}
